// @ts-ignore
/*
import { socket, friendInput } from '/includes/sharedContent'
friendInput?.addEventListener('focusin', () => {
    // prepare user options to add them as a friend 
    socket.emit("prepare user search", localStorage.getItem("id"));
    socket.on("user search", (res) => {
        res.forEach(element => {
            let dataListElement = document.getElementById("dataList")
            if (dataListElement) dataListElement.innerHTML += `<option value="${element.Username}">`;
        });
    })
})
*/